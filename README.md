# govee-proxy

A simple proxy server for Govee device integration.

Scripts

Start the proxy server with ./start_govee.sh
Stop the proxy server with ./stop_govee.sh

The proxy runs on port 5000 and provides a POST /toggle endpoint to toggle Govee devices on/off.